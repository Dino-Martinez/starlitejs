/**
* The `Layer` module contains methods for creating and manipulating body models.
* A `Layer` is a rigid body that can be simulated by a `Matter.Engine`.
* Factories for commonly used layer configurations (such as rectangles, circles and other polygons) can be found in the module `Layers`.
*
* See the included usage [examples](https://github.com/liabru/matter-js/tree/master/examples).
*/
